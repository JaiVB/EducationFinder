# Maven target directory
target/
*.jar
*.war
*.ear

# Spring Boot properties (to avoid committing sensitive data)
application.properties
application.yml
application-*.properties
application-*.yml

# Logs and temp files
logs/
*.log
*.tmp
hs_err_pid*

# Node.js dependencies and build files (for Next.js frontend)
node_modules/
.next/
out/
dist/

# Next.js environment files
.env
.env.local
.env.development
.env.production
.env.test

# MongoDB database files (if running locally)
data/db/

# IDEA and VSCode settings (already covered, but keeping for clarity)
.idea/
.vscode/

# Linting and testing cache
eslintcache/
jest-cache/
jest-test-results.json

# Coverage reports
coverage/
        